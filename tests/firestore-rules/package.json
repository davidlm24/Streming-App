{
  "name": "pwstreamer-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Testes das regras do Firestore, no emulador. Pacote separado para não mexer no lockfile do app.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.17.1"
  }
}
